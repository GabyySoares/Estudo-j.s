const number = [1,2,3];
const joint = numbers.join(','); // junta os elementos do array em uma única string, separando-os por vírgula
console.log(joined);

const message =  'This is my first message';  // declara uma constante "message" contendo uma string
const parts = message.split(''); // divide a string em um array de caracteres individuais
console.log(parts);

const combined = parts.join('-');  // junta os caracteres do array em uma única string, separando-os por hífen
console.log(combined);