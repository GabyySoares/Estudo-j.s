// Define a variável "name" e atribui o valor "mosh"
var name = 'mosh'; 
// Imprime o valor da variável "name" no console
console.log(name);


// Define duas variáveis, "firstName" e "lastName", usando "let"
let firstName = 'Mosth';
let  lastName = 'Hamedani';
// Em JavaScript, as variáveis podem ser definidas com "var" ou "let".
//A diferença entre "let" e "var" é que "let" é limitado a um bloco de código, enquanto "var" é uma variável global
