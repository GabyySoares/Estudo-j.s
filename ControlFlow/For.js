// Declaração de um loop for que começa com i = 5, testa se i >= 1, e decrementa i em 1 a cada iteração

for (let i = 5; i >= 1; i--){ 
     // Verifica se o valor de i é ímpar (ou seja, se o resto da divisão de i por 2 é diferente de zero)
     if (i % 2 !==0)
     // Se a condição for verdadeira, o valor de i é impresso no console
      console.log(i);
}